# Sprint 8 — 설정 화면 목업

## 개요

| 항목 | 내용 |
|------|------|
| 스프린트 번호 | Sprint 8 |
| 유형 | 목업 |
| 브랜치 | `sprint8` |
| 기간 | 2026-03-17 시작 |
| 상태 | 🔄 Step 1 완료 / Step 2 승인 대기 |
| 대상 브랜치 (PR) | `develop` |
| DB/백엔드 변경 | **없음** — 목업 전용 |
| 선행 조건 | Sprint 7 Step 2 승인 ✅ (2026-03-17) |

---

## 스프린트 목표

MockupView에 **설정 탭** UI/UX를 목업으로 구현한다.
카테고리 / 결제수단 / 저축 수단 관리 화면을 다크모드 팔레트로 통일하며,
결제수단 클릭 시 인라인 카드 청구 설정 또는 포인트 예산 설정이 확장되는 방식을 검증한다.

---

## Step 1 — 목업 ✅ 완료 (2026-03-17)

### 구현 파일

| 파일 | 설명 |
|------|------|
| `frontend/src/views/MockupView.vue` | 설정 탭 섹션 추가 (1560~1763줄) |

### 구현 상세

**설정 탭 구성 (`activePage === 'settings'`)**

1. **월 시작일 설정**
   - select dropdown (1~28일)
   - 변경 시 `settingMonthStartDay` 업데이트 → 거래 필터/기간 표시 즉시 반영

2. **카테고리 관리**
   - 수입 / 지출 / 저축 탭 전환 (`settingCatFilter`)
   - 카테고리별 태그 chip + 삭제 버튼 (기본값은 삭제 불가)
   - 추가 폼: 이름 입력 → 추가 버튼
   - soft delete: `isDeleted = true` (거래 목록 조회 시 표시, 신규 선택 불가)

3. **결제수단 관리**
   - 카드(신용카드/체크카드) / 현금 / 포인트 타입별 구분
   - 카드 행 클릭 → 인라인 청구 설정 전개
     - 정산일 (1~28일): 이용 마감일
     - 결제일 (1~28일): 출금일
     - "전월 XX일 ~ 당월 YY일 이용분 → ZZ일 출금" 안내 문구
   - 포인트 행 클릭 → 인라인 예산 설정 전개
     - 총 예산 / 현재 잔액 표시 및 수정
   - 현금 타입: 클릭 없음 (설정 불필요)
   - 추가 폼: 이름 + 타입 선택 → 추가

4. **저축 수단 관리**
   - 저축 계좌 목록 (기업은행, 카카오뱅크, 현금 기본값)
   - 추가 / 삭제 기능

### 주요 변경 사항 (기존 대비)

| 항목 | 내용 |
|------|------|
| 반복/할부 설정 섹션 | 설정 탭에서 제거 (홈 탭 거래 목록에서 직접 관리하는 방식으로 결정) |
| 카드 청구 설정 아코디언 | 별도 섹션 → 결제수단 행 클릭 시 인라인 전개 방식으로 변경 |
| 포인트 예산 아코디언 | 별도 섹션 → 포인트 결제수단 행 클릭 시 인라인 전개 방식으로 변경 |
| 카드 타입 | `Card` 단일 → `CreditCard` / `DebitCard` 분리 |

---

## Step 2 — UI/UX 확정 ⬜ 승인 대기

> Step 1 목업을 로컬에서 `/mock-up` → 설정 탭으로 접속하여 확인 후 승인해주세요.

### 확인 항목

| # | 항목 | 확인 포인트 |
|---|------|------------|
| 1 | 카테고리 탭 전환 | 수입/지출/저축 탭 전환 시 해당 카테고리 목록만 표시되는가? |
| 2 | 카테고리 추가/삭제 | 추가 후 목록 갱신, 기본값 삭제 불가 동작이 올바른가? |
| 3 | 카드 인라인 설정 | 카드 행 클릭 시 정산일/결제일 폼이 부드럽게 전개되는가? |
| 4 | 청구 기간 안내 | "전월 XX일 ~ 당월 YY일 이용분 → ZZ일 출금" 계산이 올바른가? |
| 5 | 포인트 인라인 설정 | 포인트 행 클릭 시 총 예산/잔액 폼이 표시되는가? |
| 6 | 저축 수단 관리 | 추가/삭제가 올바르게 동작하는가? |
| 7 | 월 시작일 변경 | 설정 변경 시 다른 탭(가계부/통계)의 기간 표시가 즉시 갱신되는가? |

### 승인 방법

> "진행해" 또는 "Step 2 완료, Sprint 9 진행해"라고 말씀해주세요.

---

## Step 3 — 실제 구현 ⬜ 보류 (Sprint 9에서 진행)

> **전략**: 설정 화면 목업(Sprint 8) 완료 후 Sprint 9에서 전체 실서비스 구현.

---

## 완료 기준

- ✅ T43: 카테고리 관리 목업 구현 (수입/지출/저축 탭 구분)
- ✅ T44: 결제수단 관리 목업 (카드/현금/포인트 타입 구분, 인라인 설정)
- ✅ T40: 카드 청구 설정 목업 (인라인 전개, 정산일/결제일)
- ✅ T41: 저축 수단 관리 목업
- ✅ `npm run build` 성공 (TypeScript 오류 0건)
- ⬜ Step 2: 사용자 UI/UX 승인
- ⬜ `sprint8 → develop` PR 생성

---

## 스프린트 회고 (완료 후 작성)

> 스프린트 완료 후 sprint-close 에이전트가 작성합니다.
